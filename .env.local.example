# Copy this file to `.env.local` (gitignored) and fill in the key below.
# Used ONLY by scripts/descriptions/* to write generated descriptions to Supabase.
# Never commit the real key — rotate it in the Supabase dashboard if it ever leaks.
#
# Where to get it: Supabase dashboard → Project Settings → API → `service_role` secret.

SUPABASE_URL=https://gebmvkghoshihlebgvxm.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
