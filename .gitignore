# Secrets — never commit. Used locally by scripts/descriptions/* to write to Supabase.
.env
.env.local
.env.*.local

# Working files for the description generation workflow (batches in flight)
scripts/descriptions/_work/

# macOS
.DS_Store
